[labels]
ssh_host = SSH Host:
username = Username:
ssh_port = SSH Port:
local_port = Local port:
remote_host = Remote host:
remote_port = Remote port:
dns_name = DNS Name:

[dialog]
base_width = 300
extra_width = 80
